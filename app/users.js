const User = require('./user');

const users = [
    new User ({title:'Alan'}),
    new User ({title:'Paul'}),
    new User ({title:'Ryan'}),
    new User ({title:'Rio'}),
    new User ({title:'Wayne'})
];

module.exports = users;
